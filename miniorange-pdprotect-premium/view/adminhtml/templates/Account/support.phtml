<?php
/**
 * Contact Us / Submit Query panel — appears on the right of every Account page state.
 * Mirrors SSO's Account/support.phtml exactly (layout block is rendered before the main
 * account block so the pull-right float places it on the right side).
 *
 * @var \MiniOrange\PDProtectPremium\ViewModel\Adminhtml\Account $viewModel
 */
$viewModel  = $this->getData('view_model');
$formKey    = $this->getBlockHtml('formkey');
$supportUrl = $viewModel->getSupportUrl();

// Positioning handled by .mopdp-account-support flex column in account.phtml —
// no panel-heading / pull-right wrapper needed.
echo '
<div class="bg-gray-50 py-6 rounded-lg shadow support_outer_div" id="minisupport">
    <div class="mosp_table_layout">
        <div class="p-2 support_heading"> Contact Us</div>
        <hr>
        <div class="flex">
            <form name="submit_form" method="post" action="' . $supportUrl . '">
                ' . $formKey . '
                <h3 class="support_needHelp">
                    Need any help? Just send us a query so we can assist you.
                </h3>
                <input type="hidden" name="option" value="supportQuery">
                <div>
                    <table style="width:100%">
                        <tr class="my-4">
                            <td class="mo-input-wrapper group my-6 support_padding">
                                <label class="mo-input-label_support">Email <span class="mo-support-required">*</span></label>
                                <input class="w-full mo-input border border-gray-300 rounded-lg p-2" id="query_email" type="email" name="email" value="" required placeholder="Enter your Email">
                            </td>
                        </tr>
                        <tr class="my-4">
                            <td class="mo-input-wrapper group my-6 support_padding">
                                <label class="mo-input-label_support">Phone</label>
                                <input id="query_phone" class="w-full mo-input border border-gray-300 rounded-lg p-2" type="text" name="phone" title="Phone with country code eg. +1xxxxxxxxxx" placeholder="Phone with country code eg. +1xxxxxxxxxx" value="" pattern="[\+]?[0-9]{1,4}\s?[0-9]{10}">
                            </td>
                        </tr>
                        <tr class="my-4">
                            <td class="mo-input-wrapper group my-6 support_padding">
                                <label class="mo-input-label_support" for="query">How can we help you? <span class="mo-support-required">*</span></label>
                                <textarea id="query" name="query"
                                    class="mo-support-query resize-y h-28 w-full p-2 border border-gray-300 rounded-lg mo-input"
                                    placeholder="Write your query here" required></textarea>
                            </td>
                        </tr>
                    </table>
                </div>
                <input type="submit" class="rounded-lg font-semibold text-white py-2 px-4 margin_left_support"
                       name="send_query" id="send_query" value="Submit Query">
            </form>
        </div>
        <br>
    </div>
    <br>
</div>

';
?>
